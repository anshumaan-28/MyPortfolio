const fs = require("fs"); const content = fs.readFileSync("src/app/page.tsx", "utf8"); const newContent = content.replace(/<motion\.button\s+onClick={scrollToAbout}\s+whileHover={{ scale: 1\.05 }}\s+whileTap={{ scale: 0\.95 }}\s+className="px-6 py-3 bg-foreground text-background rounded-md font-medium"\s*>\s*View My Work\s*<\/motion\.button>/, fs.readFileSync("temp.txt", "utf8")); fs.writeFileSync("src/app/page.tsx", newContent);
